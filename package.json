{
  "name": "fitness-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "npm run clean --prefix Fitness",
    "dev": "npm run dev --prefix Fitness",
    "dev:all": "npm run dev:all --prefix Fitness",
    "backend": "npm run backend --prefix Fitness",
    "frontend": "npm run frontend --prefix Fitness",
    "start": "npm run start --prefix Fitness",
    "build": "npm run build --prefix Fitness"
  }
}
