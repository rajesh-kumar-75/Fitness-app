{
  "name": "fitness-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack Fitness and Workout Management Platform",
  "scripts": {
    "dev": "npm run dev:all",
    "dev:all": "npx --yes concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "start": "npm run dev:all",
    "build": "npm run build --prefix frontend",
    "clean": "npx --yes kill-port 5000 4200"
  }
}
